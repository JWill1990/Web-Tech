.marquee {
    width: 300px;
    height: 20px;
    float: right;
    margin: 0 auto;
    clear: both;
    margin-top: 10px;
    margin-bottom: 10px;
    background-color: rgba(127,127,250,0.5);
    border-radius: 6px;
    color: black;
    white-space: nowrap;
    text-shadow: 1px 1px #ff0000;	
    overflow: hidden;
}

.marquee span {
    display: inline-block;
    padding-left: 100%;
    text-indent: 0;
    animation: marquee 20s linear infinite;
}

.marquee span:hover {
    animation-play-state: paused
}

@keyframes marquee {
    0%   { transform: translate(0, 0); }
    100% { transform: translate(-100%, 0); }
}
