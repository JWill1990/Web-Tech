#slide {
    border-radius: 8px;
    box-shadow: 0 0 15px 1px rgba(0, 0, 0, 0.4);
    margin-left:auto;
    margin-right:auto;
    display:block;
    width: 80%;
    min-width: 640px;
    max-width: 900px;
    align: center;
    opacity:1;
    transition: opacity 1s; 
}

#slide.fadeOut {
    opacity:0;
}
